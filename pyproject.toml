[project]
name = "flask-web-sample"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
license-files = ["LICENSE"]
dependencies = [
    "flask>=3.1.1",
    "pytest>=8.4.1",
]


[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
